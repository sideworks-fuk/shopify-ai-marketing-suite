name: Production Backend Deploy

on:
  workflow_dispatch:
    inputs:
      deploy_message:
        description: 'デプロイメッセージ（任意）'
        required: false
        type: string
      confirm_production:
        description: '本番環境へのデプロイを確認'
        required: true
        type: choice
        options:
          - 'YES - 本番環境にデプロイします'
          - 'NO - キャンセルします'
        default: 'NO - キャンセルします'
      use_deployment_slot:
        description: 'デプロイスロットを使用（推奨: ダウンタイムなし）'
        required: true
        type: choice
        options:
          - 'YES - デプロイスロットを使用（推奨）'
          - 'NO - 直接デプロイ（停止が必要）'
        default: 'YES - デプロイスロットを使用（推奨）'

env:
  DOTNET_VERSION: '8.x'
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages
  APP_NAME: 'ec-ranger-backend-prod'  # Japan West の App Service
  APP_URL: 'https://ec-ranger-backend-prod-ghf3bbarghcwh4gn.japanwest-01.azurewebsites.net/'

jobs:
  production-deploy:
    runs-on: windows-latest
    environment: ec-ranger-prod
    
    steps:
    - name: 🛡️ Production deployment confirmation
      if: github.event.inputs.confirm_production != 'YES - 本番環境にデプロイします'
      run: |
        echo "❌ Production deployment cancelled by user"
        exit 1
      shell: bash

    - name: 📥 Checkout repository
      uses: actions/checkout@v4

    - name: 🔧 Setup .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 📦 Setup NuGet cache
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/packages.lock.json', '**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: 🔧 Configure NuGet
      run: |
        # NuGet source is already configured in NuGet.Config file
        dotnet nuget list source
      shell: pwsh

    - name: 🔐 Azure Login
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
      if: |
        github.event.inputs.use_deployment_slot == 'YES - デプロイスロットを使用（推奨）' ||
        github.event.inputs.use_deployment_slot == 'NO - 直接デプロイ（停止が必要）'

    - name: 📝 Display deployment info
      run: |
        echo "🚀 Production Backend Deployment Started"
        echo "Environment: PRODUCTION (Japan West)"
        echo "Triggered by: ${{ github.actor }}"
        echo "Deploy message: ${{ github.event.inputs.deploy_message }}"
        echo "Timestamp: $(date)"
        echo "Target App: ${{ env.APP_NAME }}"
        echo "Deployment Method: ${{ github.event.inputs.use_deployment_slot }}"
        if [ "${{ github.event.inputs.use_deployment_slot }}" = "YES - デプロイスロットを使用（推奨）" ]; then
          echo "✅ Using deployment slot (zero downtime)"
        else
          echo "⚠️ Direct deployment (requires app stop)"
        fi
        echo "⚠️ WARNING: This will deploy to the PRODUCTION environment!"
      shell: bash

    - name: 🔍 Restore dependencies (with retry)
      run: |
        cd backend/ShopifyAnalyticsApi
        $maxRetries = 3
        $retryCount = 0
        $success = $false
        
        while (-not $success -and $retryCount -lt $maxRetries) {
          try {
            $retryCount++
            Write-Host "Attempt $retryCount of $maxRetries..."
            
            # Clear NuGet caches if retry
            if ($retryCount -gt 1) {
              dotnet nuget locals all --clear
            }
            
            # Restore with timeout and retry settings
            dotnet restore --disable-parallel --verbosity normal
            
            $success = $true
            Write-Host "✅ Dependencies restored successfully!"
          }
          catch {
            Write-Host "❌ Restore failed on attempt $retryCount"
            Write-Host $_.Exception.Message
            
            if ($retryCount -eq $maxRetries) {
              throw "Failed to restore dependencies after $maxRetries attempts"
            }
            
            Write-Host "Waiting 10 seconds before retry..."
            Start-Sleep -Seconds 10
          }
        }
      shell: pwsh

    - name: 🏗️ Build project
      run: |
        cd backend/ShopifyAnalyticsApi
        dotnet build --configuration Release --no-restore

    # - name: 🧪 Run tests
    #   run: |
    #     cd backend
    #     if (Test-Path "*.Tests") {
    #       dotnet test --configuration Release --no-build --verbosity normal
    #     } else {
    #       Write-Host "No test projects found, skipping tests"
    #     }
    #   shell: pwsh
    # Note: Tests temporarily disabled for production deployment - will re-enable after environment stabilization

    - name: 📦 Publish project
      run: |
        cd backend/ShopifyAnalyticsApi
        dotnet publish -c Release -o ./published --no-restore --no-build

    - name: 🔍 Verify published files before deployment
      run: |
        cd backend/ShopifyAnalyticsApi/published
        Write-Host "📦 Published files:"
        Get-ChildItem -Filter "*.dll" | Select-Object Name, LastWriteTime | Format-Table
        Write-Host "📄 Configuration files:"
        Get-ChildItem -Filter "appsettings*.json" | Select-Object Name, LastWriteTime | Format-Table
        Write-Host "✅ Verification complete"
      shell: pwsh

    # デプロイスロットを使用する場合（推奨: ダウンタイムなし）
    - name: 🚀 Deploy to Staging Slot
      if: github.event.inputs.use_deployment_slot == 'YES - デプロイスロットを使用（推奨）'
      uses: azure/webapps-deploy@v3
      with:
        app-name: ${{ env.APP_NAME }}
        slot-name: 'staging'
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_PRODUCTION_STAGING }}
        package: backend/ShopifyAnalyticsApi/published
      continue-on-error: false

    - name: 🏥 Health check (Staging Slot)
      if: github.event.inputs.use_deployment_slot == 'YES - デプロイスロットを使用（推奨）'
      run: |
        echo "Waiting 30 seconds for staging deployment to stabilize..."
        Start-Sleep -Seconds 30
        echo "Performing health check on staging slot..."
        $stagingUrl = "https://ec-ranger-backend-prod-staging-ghf3bbarghcwh4gn.japanwest-01.azurewebsites.net/api/health"
        try {
          $response = Invoke-WebRequest -Uri $stagingUrl -UseBasicParsing -TimeoutSec 30
          if ($response.StatusCode -eq 200) {
            echo "✅ Staging slot health check passed!"
          } else {
            echo "⚠️ Staging slot health check returned status: $($response.StatusCode)"
          }
        } catch {
          echo "❌ Staging slot health check failed: $_"
          echo "⚠️ Note: Proceeding with swap, but please verify manually"
        }
      shell: pwsh

    - name: 🔄 Swap slots (Production ↔ Staging)
      if: github.event.inputs.use_deployment_slot == 'YES - デプロイスロットを使用（推奨）'
      uses: azure/CLI@v2
      with:
        inlineScript: |
          echo "🔄 Swapping slots: staging ↔ production"
          az webapp deployment slot swap \
            --resource-group ec-ranger-prod \
            --name ${{ env.APP_NAME }} \
            --slot staging \
            --target-slot production
          echo "✅ Slot swap completed successfully!"

    # 直接デプロイする場合（app_offline.htmを使用してファイルロックを回避）
    - name: 📄 Create app_offline.htm
      if: github.event.inputs.use_deployment_slot == 'NO - 直接デプロイ（停止が必要）'
      run: |
        echo "<html><head><title>Updating...</title><meta http-equiv='refresh' content='10'></head><body style='font-family: Arial, sans-serif; text-align: center; padding: 50px;'><h1>Application is being updated</h1><p>Please wait while we update the application. This page will automatically refresh.</p><p>If this page does not refresh automatically, please try again in a few moments.</p></body></html>" > app_offline.htm
        echo "✅ app_offline.htm created"
      shell: bash

    - name: 🚀 Deploy app_offline.htm (Shutdown App)
      if: github.event.inputs.use_deployment_slot == 'NO - 直接デプロイ（停止が必要）'
      uses: azure/webapps-deploy@v3
      with:
        app-name: ${{ env.APP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_PRODUCTION }}
        package: app_offline.htm
      continue-on-error: false

    - name: ⏳ Wait for app shutdown
      if: github.event.inputs.use_deployment_slot == 'NO - 直接デプロイ（停止が必要）'
      run: |
        echo "⏳ Waiting 15 seconds for application to shut down..."
        sleep 15
        echo "✅ Application shutdown complete (file locks should be released)"
      shell: bash

    - name: 🚀 Deploy application
      if: github.event.inputs.use_deployment_slot == 'NO - 直接デプロイ（停止が必要）'
      uses: azure/webapps-deploy@v3
      with:
        app-name: ${{ env.APP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_PRODUCTION }}
        package: backend/ShopifyAnalyticsApi/published
      continue-on-error: false

    - name: 🗑️ Remove app_offline.htm (Restart App)
      if: github.event.inputs.use_deployment_slot == 'NO - 直接デプロイ（停止が必要）'
      uses: azure/CLI@v2
      with:
        inlineScript: |
          echo "🗑️ Removing app_offline.htm to restart the application..."
          az rest --method DELETE \
            --uri "https://${{ env.APP_NAME }}.scm.azurewebsites.net/api/vfs/site/wwwroot/app_offline.htm" \
            --headers "Authorization=Bearer $(az account get-access-token --query accessToken -o tsv)" || echo "⚠️ app_offline.htm may have been automatically removed"
          echo "✅ Application should restart automatically"
      env:
        AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}

    - name: ✅ Deployment complete
      run: |
        echo "✅ Production backend deployment completed successfully!"
        echo "🌐 App URL: ${{ env.APP_URL }}"
        echo "📊 Check Azure Portal for detailed logs"
        echo "⚠️ PRODUCTION environment is now live!"
        echo ""
        if [ "${{ github.event.inputs.use_deployment_slot }}" = "YES - デプロイスロットを使用（推奨）" ]; then
          echo "✅ Deployment method: Deployment Slot (Zero Downtime)"
          echo "   - Deployed to staging slot"
          echo "   - Swapped to production slot"
        else
          echo "⚠️ Deployment method: Direct Deployment"
          echo "   - App may have been stopped during deployment"
        fi
        echo ""
        echo "🔍 Next steps:"
        echo "1. Verify deployment in Azure Portal → Deploy → Deployment Center"
        echo "2. Check file timestamps in Kudu (Advanced Tools)"
        echo "3. Verify DLL files are updated: D:/home/site/wwwroot/"
        echo "4. Check appsettings.Production.json is updated"
      shell: bash

    - name: 🏥 Health check
      run: |
        echo "Waiting 45 seconds for deployment to stabilize..."
        Start-Sleep -Seconds 45
        echo "Performing health check..."
        try {
          $response = Invoke-WebRequest -Uri "${{ env.APP_URL }}api/health" -UseBasicParsing -TimeoutSec 30
          if ($response.StatusCode -eq 200) {
            echo "✅ Health check passed!"
          } else {
            echo "⚠️ Health check returned status: $($response.StatusCode)"
          }
        } catch {
          echo "❌ Health check failed: $_"
          echo "⚠️ Note: Please check the application manually"
        }
      shell: pwsh

    - name: 📋 Production deployment summary
      if: always()
      run: |
        echo "================================================"
        echo "Production Deployment Summary"
        echo "================================================"
        echo "Environment: PRODUCTION"
        echo "Region: Japan West"
        echo "App Service: ${{ env.APP_NAME }}"
        echo "URL: ${{ env.APP_URL }}"
        echo "Deployed by: ${{ github.actor }}"
        echo "Deployment time: $(date)"
        echo "================================================"
