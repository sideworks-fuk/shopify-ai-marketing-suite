# マルチアプリ設定管理ドキュメント

## 概要

Shopify AI Marketing Suite は、複数のShopifyアプリ（公開アプリ・カスタムアプリ）をサポートするマルチアプリ構成です。このドキュメントでは、各アプリの設定情報を一元管理します。

**最終更新**: 2025-12-24  
**作成者**: 福田 + AI Assistant

---

## 📋 目次

1. [現在のアプリ構成](#現在のアプリ構成)
2. [ShopifyAppsテーブル設定](#shopifyappsテーブル設定)
3. [Shopify Partners Dashboard設定](#shopify-partners-dashboard設定)
4. [Azure Static Web Apps設定](#azure-static-web-apps設定)
5. [GitHub Actions環境変数設定](#github-actions環境変数設定)
6. [今後の追加予定アプリ](#今後の追加予定アプリ)
7. [設定変更時の手順](#設定変更時の手順)

---

## 現在のアプリ構成

### アプリ一覧

| アプリ名 | アプリタイプ | 対象ストア | フロントエンド | ステータス |
|---------|------------|-----------|--------------|-----------|
| EC Ranger-demo | Custom | roomandout.myshopify.com | white-island | ✅ 稼働中 |
| EC Ranger-xn-fbkq6e5da0fpb | Custom | xn-fbkq6e5da0fpb.myshopify.com | black-flower | ✅ 稼働中 |
| EC Ranger | Public | 一般公開（審査中） | 未定 | ⏳ 準備中 |

### 環境マッピング

```
white-island (ec-ranger-frontend-prod)
  └─ EC Ranger-demo
     └─ roomandout.myshopify.com（お客様用）

black-flower (ec-ranger-frontend-prod-2)
  └─ EC Ranger-xn-fbkq6e5da0fpb
     └─ xn-fbkq6e5da0fpb.myshopify.com（検証用ストア）
```

---

## ShopifyAppsテーブル設定

### データベース: `ec-ranger-sql-prod`

#### EC Ranger-demo (Id: 5)

```sql
Id: 5
Name: EC Ranger-demo
DisplayName: EC Ranger-demo
AppType: Custom
ApiKey: 23f81e22074df1b71fb0a5a495778f49
ApiSecret: shpss_*** (機密情報のためマスク)
AppUrl: https://white-island-08e0a6300.2.azurestaticapps.net
RedirectUri: https://white-island-08e0a6300.2.azurestaticapps.net/api/shopify/callback
Scopes: read_orders,read_products,read_customers
IsActive: true
CreatedAt: 2025-12-22
UpdatedAt: 2025-12-24
```

#### EC Ranger-xn-fbkq6e5da0fpb (Id: 6)

```sql
Id: 6
Name: EC Ranger-xn-fbkq6e5da0fpb
DisplayName: EC Ranger-xn-fbkq6e5da0fpb
AppType: Custom
ApiKey: 706a757915dedce54806c0a179bee05d
ApiSecret: shpss_*** (機密情報のためマスク)
AppUrl: https://black-flower-004e1de00.2.azurestaticapps.net
RedirectUri: https://black-flower-004e1de00.2.azurestaticapps.net/api/shopify/callback
Scopes: read_orders,read_products,read_customers
IsActive: true
CreatedAt: 2025-12-22
UpdatedAt: 2025-12-24
```

### 確認クエリ

```sql
-- 全アプリ一覧
SELECT 
    Id,
    Name,
    DisplayName,
    AppType,
    ApiKey,
    AppUrl,
    RedirectUri,
    IsActive,
    CreatedAt,
    UpdatedAt
FROM [dbo].[ShopifyApps]
ORDER BY AppType, Id;
```

---

## Shopify Partners Dashboard設定

### EC Ranger-demo

**App URL**: `https://white-island-08e0a6300.2.azurestaticapps.net` ⚠️ **重要**: `/install` ではなく `/` に設定すること

**⚠️ 現在の問題**: App URLが `/install` になっているため、404エラーが発生している可能性があります。必ず `/` に設定してください。

**Redirect URLs**:
- `https://white-island-08e0a6300.2.azurestaticapps.net/api/shopify/callback`
- `https://white-island-08e0a6300.2.azurestaticapps.net/auth/success`
- `https://ec-ranger-backend-prod-ghf3bbarghcwh4gn.japanwest-01.azurewebsites.net/api/shopify/callback`

**Credentials**:
- Client ID: `23f81e22074df1b71fb0a5a495778f49`
- Secret: `shpss_***` (機密情報のためマスク、実際の値はShopify Partners Dashboardで確認) (Created: Dec 22, 2025 2:47 pm)

**Install Link**:
```
https://admin.shopify.com/oauth/install_custom_app?client_id=23f81e22074df1b71fb
```

**その他の設定**:
- Use legacy install flow: `true`
- Embed app in Shopify admin: `true`
- API version: `2025-10`

---

### EC Ranger-xn-fbkq6e5da0fpb

**App URL**: `https://black-flower-004e1de00.2.azurestaticapps.net` ⚠️ **重要**: `/install` ではなく `/` に設定すること

**Redirect URLs**:
- `https://black-flower-004e1de00.2.azurestaticapps.net/api/shopify/callback`
- `https://black-flower-004e1de00.2.azurestaticapps.net/auth/success`
- `https://ec-ranger-backend-prod-ghf3bbarghcwh4gn.japanwest-01.azurewebsites.net/api/shopify/callback`

**Credentials**:
- Client ID: `706a757915dedce54806c0a179bee05d`
- Secret: `shpss_***` (機密情報のためマスク、実際の値はShopify Partners Dashboardで確認) (Created: Dec 23, 2025 3:02 pm)

**Install Link**:
```
https://admin.shopify.com/oauth/install_custom_app?client_id=706a757915dedce548
```

**その他の設定**:
- Use legacy install flow: `false`
- Embed app in Shopify admin: `true`
- API version: `2025-10`
- カスタム配布: Plus組織内の複数ストアへのインストールを許可

---

## Azure Static Web Apps設定

### white-island (ec-ranger-frontend-prod)

**リソース名**: `ec-ranger-frontend-prod`  
**URL**: `https://white-island-08e0a6300.2.azurestaticapps.net`  
**SKU**: `Standard`  
**リソースグループ**: `ec-ranger-prod`  
**サブスクリプション**: `アクセスネット`  
**ソース**: `main (GitHub)`  
**ワークフロー**: `azure-static-web-apps-white-island-08e0a6300.yml`

**環境変数**:
- `NEXT_PUBLIC_SHOPIFY_API_KEY`: `23f81e22074df1b71fb0a5a495778f49` (EC Ranger-demo用)

**⚠️ 補足**: GitHub Actionsのワークフローでビルド時に環境変数が設定されているため、実行時にAzure Static Web Appsで設定されていなくても動作します。ただし、将来のデプロイで環境変数が正しく設定されていることを確認するため、Azure Portalでも設定しておくことを推奨します。

---

### black-flower (ec-ranger-frontend-prod-2)

**リソース名**: `ec-ranger-frontend-prod-2`  
**URL**: `https://black-flower-004e1de00.2.azurestaticapps.net`  
**SKU**: `Free`  
**リソースグループ**: `ec-ranger-prod`  
**サブスクリプション**: `アクセスネット`  
**ソース**: `main (GitHub)`  
**ワークフロー**: `azure-static-web-apps-black-flower-004e1de00.yml`

**環境変数**:
- `NEXT_PUBLIC_SHOPIFY_API_KEY`: `706a757915dedce54806c0a179bee05d` (EC Ranger-xn-fbkq6e5da0fpb用)

**⚠️ 補足**: GitHub Actionsのワークフローでビルド時に環境変数が設定されているため、実行時にAzure Static Web Appsで設定されていなくても動作します。ただし、将来のデプロイで環境変数が正しく設定されていることを確認するため、Azure Portalでも設定しておくことを推奨します。

---

## GitHub Actions環境変数設定

### Production Frontend Workflow (`.github/workflows/production_frontend.yml`)

#### Production1 (white-island)

```yaml
env:
  PROD1_FRONTEND_URL: 'https://white-island-08e0a6300.2.azurestaticapps.net'
  PROD1_SHOPIFY_API_KEY: ${{ secrets.SHOPIFY_API_KEY_PRODUCTION || vars.SHOPIFY_API_KEY_PRODUCTION }}
  
# deploy-frontend job内
env:
  NEXT_PUBLIC_SHOPIFY_API_KEY: '${{ env.PROD1_SHOPIFY_API_KEY }}'
```

**GitHub Secrets/Variables**:
- `SHOPIFY_API_KEY_PRODUCTION`: `23f81e22074df1b71fb0a5a495778f49` (EC Ranger-demo用)

---

#### Production2 (black-flower)

```yaml
env:
  PROD2_FRONTEND_URL: 'https://black-flower-004e1de00.2.azurestaticapps.net'
  PROD2_SHOPIFY_API_KEY: ${{ secrets.SHOPIFY_API_KEY_PRODUCTION_2 || vars.SHOPIFY_API_KEY_PRODUCTION_2 }}
  
# deploy-frontend job内
env:
  NEXT_PUBLIC_SHOPIFY_API_KEY: '${{ env.PROD2_SHOPIFY_API_KEY }}'
```

**GitHub Secrets/Variables**:
- `SHOPIFY_API_KEY_PRODUCTION_2`: `706a757915dedce54806c0a179bee05d` (EC Ranger-xn-fbkq6e5da0fpb用)

---

## 今後の追加予定アプリ

### 1. 追加のカスタムアプリ

**用途**: 新規顧客向けカスタムアプリ  
**ステータス**: ⏳ 計画中

**必要な設定**:
- Shopify Partners Dashboardで新規カスタムアプリを作成
- `ShopifyApps`テーブルにレコード追加
- Azure Static Web Appsの新規作成（または既存の再利用）
- GitHub Actions環境変数の追加

---

### 2. EC Ranger（公開アプリ）

**用途**: Shopify App Storeへの一般公開用  
**ステータス**: ⏳ 準備中（審査待ち）

**必要な設定**:
- Shopify Partners Dashboardで公開アプリを作成
- `ShopifyApps`テーブルにレコード追加（`AppType: Public`）
- Azure Static Web Appsの新規作成（公開アプリ専用）
- GitHub Actions環境変数の追加
- Shopify App Store審査用の設定

**注意事項**:
- 公開アプリはShopify App Store審査が必要
- App URLは公開アプリ専用のものを使用（カスタムアプリと共有不可）
- スコープの最小化が必要（審査要件）

---

## 設定変更時の手順

### 1. ShopifyAppsテーブルの更新

```sql
-- 例: AppUrlの変更
UPDATE [dbo].[ShopifyApps]
SET 
    [AppUrl] = '新しいAppUrl',
    [RedirectUri] = '新しいRedirectUri',
    [UpdatedAt] = GETUTCDATE()
WHERE [Id] = [アプリID];
```

**確認**:
```sql
SELECT * FROM [dbo].[ShopifyApps] WHERE [Id] = [アプリID];
```

---

### 2. Shopify Partners Dashboardの更新

1. Shopify Partners Dashboardにログイン
2. 対象アプリの「Settings」ページを開く
3. 以下の設定を更新:
   - **App URL**: 新しいApp URLを設定
   - **Redirect URLs**: 新しいRedirect URLを追加（既存のものは削除しない）
4. 変更を保存

**⚠️ 重要: App URLの設定**
- **App URLは `/` に設定すること**（例: `https://white-island-08e0a6300.2.azurestaticapps.net`）
- `/install` に設定すると、認証済みユーザーがメニューからアプリを呼び出すと常にインストール画面に飛んでしまう
- `/` ページは認証状態をチェックして、認証済みなら `/customers/dormant` に、未認証なら `/install` に自動リダイレクトする

**注意事項**:
- Redirect URLsの変更は即座に反映される
- App URLの変更は新しいバージョンのリリースが必要な場合がある

---

### 3. Azure Static Web Appsの環境変数更新

1. Azure PortalでStatic Web Appを開く
2. 「設定」→「環境変数」を選択
3. `NEXT_PUBLIC_SHOPIFY_API_KEY` を更新
4. 変更を保存（自動的に再デプロイが実行される）

**または、GitHub Actions環境変数を更新**:
1. GitHubリポジトリの「Settings」→「Secrets and variables」→「Actions」を開く
2. 対象のSecret/Variableを更新
3. 手動でワークフローを再実行

---

### 4. 動作確認

1. **フロントエンドの確認**:
   - ブラウザのConsoleで `localStorage.getItem('oauth_debug_info')` を実行
   - `apiKey` が正しいか確認

2. **バックエンドログの確認**:
   - Azure App Serviceのログストリームで以下を確認:
     - `ShopifyAppテーブルからCredentialsを取得`
     - `OAuth redirect_uri決定`

3. **OAuth認証の確認**:
   - インストールフローを実行
   - 認証成功画面が表示されることを確認

---

## トラブルシューティング

### 問題: `redirect_uri is not whitelisted`

**原因**: Shopify Partners DashboardのRedirect URLsに該当URLが含まれていない

**解決方法**:
1. Shopify Partners Dashboardでアプリの「Settings」を開く
2. 「Redirect URLs」に該当URLを追加
3. 保存して再試行

---

### 問題: `API Secret not found for the provided API Key`

**原因**: `ShopifyApps`テーブルに該当する`ApiKey`が存在しない、または`IsActive=false`

**解決方法**:
1. デバッグエンドポイントで確認: `/api/shopify/debug-shopify-apps`
2. `ShopifyApps`テーブルを確認:
   ```sql
   SELECT * FROM [dbo].[ShopifyApps] WHERE [ApiKey] = '対象のApiKey';
   ```
3. レコードが存在しない場合は追加、`IsActive=false`の場合は`true`に更新

---

### 問題: バックエンドが間違った`AppUrl`を返す

**原因**: `ShopifyApps`テーブルの`AppUrl`が古い値のまま

**解決方法**:
1. `ShopifyApps`テーブルの`AppUrl`を確認
2. 正しい値に更新（上記「設定変更時の手順」参照）

---

## 参考リンク

- [ShopifyAppsテーブル設計書](../../03-feature-development/マルチアプリ対応/ShopifyAppsテーブル設計書.md)
- [マルチアプリ対応実装方針決定書](../../03-feature-development/マルチアプリ対応/実装方針決定書.md)
- [Shopify OAuth実装ガイド](../archive/oauth-legacy/shopify-oauth-implementation-guide.md)

---

## 更新履歴

| 日付 | 更新内容 | 更新者 |
|------|---------|--------|
| 2025-12-24 | 初版作成（EC Ranger-demo, EC Ranger-xn-fbkq6e5da0fpbの設定を追加） | 福田 + AI Assistant |
| 2025-12-25 | 404エラー原因特定（App URL設定と環境変数の未設定） | 福田 + AI Assistant |

