# 注文日時フィールド参照先確認結果

**作成日**: 2026-01-20  
**更新日**: 2026-01-22（訂正）  
**目的**: EC Rangerアプリで使用している注文日時フィールド（`processedAt`と`createdAt`）の参照先を確認

---

## 🔴 訂正事項（2026-01-22追記）

> **重要な訂正**: 本ドキュメントの以下の記述は誤りでした。
> 
> ~~`processed_at`フィールドはREST APIには存在しません。`processedAt`は**Shopify Admin GraphQL API**のフィールドです。~~
> 
> **正しい情報**: Shopify REST API の Order オブジェクトには `processed_at` フィールドが**存在します**。GraphQL対応は不要で、REST APIのまま対応可能です。
> 
> **対応完了**: 2026-01-22に REST API から `processed_at` を取得・保存する実装を完了しました。

---

## 📋 確認結果サマリー

### 結論

~~**EC Rangerアプリは、現在 `createdAt`（Shopify REST APIの`created_at`）を参照しています。**~~

~~`processedAt`（Shopify Admin GraphQL APIの`processedAt`）は**現在取得・保存していません**。~~

**【2026-01-22 訂正】EC Rangerアプリは、`processedAt`（Shopify REST APIの`processed_at`）を取得・保存するよう対応完了しました。**

---

## 🔍 詳細確認結果

### 1. Shopify APIからの取得

**実装箇所**: `backend/ShopifyAnalyticsApi/Services/ShopifyApiService.cs`

~~```1010:1013:backend/ShopifyAnalyticsApi/Services/ShopifyApiService.cs
[JsonPropertyName("created_at")]
public DateTime? CreatedAt { get; set; }
[JsonPropertyName("updated_at")]
public DateTime? UpdatedAt { get; set; }
```~~

**【2026-01-22 訂正後】**:
```csharp
[JsonPropertyName("created_at")]
public DateTime? CreatedAt { get; set; }
[JsonPropertyName("updated_at")]
public DateTime? UpdatedAt { get; set; }
/// <summary>
/// 決済完了日時（分析レポートで優先使用すべき日時）
/// </summary>
[JsonPropertyName("processed_at")]
public DateTime? ProcessedAt { get; set; }
```

**確認事項**:
- ✅ `created_at`フィールドを取得している
- ~~❌ `processed_at`フィールドは取得していない~~
- ✅ **【2026-01-22 訂正】** `processed_at`フィールドを取得するよう対応完了

~~**注**: 現在の実装では**Shopify REST API**を使用しており、`processed_at`フィールドはREST APIには存在しません。`processedAt`は**Shopify Admin GraphQL API**のフィールドです。~~

**【2026-01-22 訂正】** Shopify REST API の Order オブジェクトには `processed_at` フィールドが存在します。GraphQL対応は不要です。

### 2. データベースへの保存

**実装箇所**: `backend/ShopifyAnalyticsApi/Jobs/ShopifyOrderSyncJob.cs`

~~```361:362:backend/ShopifyAnalyticsApi/Jobs/ShopifyOrderSyncJob.cs
ShopifyCreatedAt = shopifyOrder.CreatedAt,
ShopifyUpdatedAt = shopifyOrder.UpdatedAt,
```~~

**【2026-01-22 訂正後】**:
```csharp
ShopifyCreatedAt = shopifyOrder.CreatedAt,
ShopifyUpdatedAt = shopifyOrder.UpdatedAt,
ShopifyProcessedAt = shopifyOrder.ProcessedAt, // 決済完了日時
```

**確認事項**:
- ✅ `ShopifyCreatedAt`に`shopifyOrder.CreatedAt`（Shopify REST APIの`created_at`）を保存
- ~~❌ `processedAt`フィールドは保存していない~~
- ✅ **【2026-01-22 訂正】** `ShopifyProcessedAt`に`shopifyOrder.ProcessedAt`を保存するよう対応完了

**データベーススキーマ**: `backend/ShopifyAnalyticsApi/Models/DatabaseModels.cs`

~~```256:258:backend/ShopifyAnalyticsApi/Models/DatabaseModels.cs
// Shopify側の作成/更新日時（分析用途）
public DateTime? ShopifyCreatedAt { get; set; }
public DateTime? ShopifyUpdatedAt { get; set; }
```~~

**【2026-01-22 訂正後】**:
```csharp
// Shopify側の作成/更新日時（分析用途）
public DateTime? ShopifyCreatedAt { get; set; }
public DateTime? ShopifyUpdatedAt { get; set; }

/// <summary>
/// Shopify側の決済完了日時（分析で優先使用）
/// 分析レポートではCreatedAtよりもProcessedAtを使用すべき
/// </summary>
public DateTime? ShopifyProcessedAt { get; set; }
```

**確認事項**:
- ✅ `ShopifyCreatedAt`フィールドが存在
- ~~❌ `ProcessedAt`フィールドは存在しない~~
- ✅ **【2026-01-22 訂正】** `ShopifyProcessedAt`フィールドを追加完了

### 3. 分析処理での使用

#### 3.1. 休眠顧客の判定（最終注文日）

**実装箇所**: `backend/ShopifyAnalyticsApi/Services/Dormant/DormantCustomerQueryService.cs`

~~```113:114:backend/ShopifyAnalyticsApi/Services/Dormant/DormantCustomerQueryService.cs
.OrderByDescending(o => o.ShopifyCreatedAt ?? o.CreatedAt)
.Select(o => (DateTime?)(o.ShopifyCreatedAt ?? o.CreatedAt))
```~~

**【2026-01-22 訂正後】**:
```csharp
.OrderByDescending(o => o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt)
.Select(o => (DateTime?)(o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt))
```

**使用フィールド**: ~~`ShopifyCreatedAt ?? CreatedAt`~~ → `ShopifyProcessedAt ?? ShopifyCreatedAt ?? CreatedAt`
- `ShopifyProcessedAt`が存在する場合はそれを使用（最優先）
- 存在しない場合は`ShopifyCreatedAt`を使用
- それも存在しない場合は`CreatedAt`（システム作成日時）を使用

#### 3.2. 期間集計/一覧の対象日

**実装箇所**: `backend/ShopifyAnalyticsApi/Controllers/DashboardController.cs`

~~```94:94:backend/ShopifyAnalyticsApi/Controllers/DashboardController.cs
.GroupBy(o => (o.ShopifyCreatedAt ?? o.CreatedAt).Date)
```~~

**【2026-01-22 訂正後】**:
```csharp
.GroupBy(o => (o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt).Date)
```

**使用フィールド**: ~~`ShopifyCreatedAt ?? CreatedAt`~~ → `ShopifyProcessedAt ?? ShopifyCreatedAt ?? CreatedAt`
- すべての期間集計・一覧処理で同じパターンに更新済み

---

## 📊 使用箇所一覧（2026-01-22 更新）

### 休眠顧客関連（すべて対応完了 ✅）

1. **`DormantCustomerQueryService.cs`**
   - 最終注文日の取得: ~~`o.ShopifyCreatedAt ?? o.CreatedAt`~~ → `o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt`

2. **`CustomerController.cs`**
   - 休眠顧客の判定: ~~`o.ShopifyCreatedAt ?? o.CreatedAt`~~ → `o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt`

3. **`DormantStatisticsService.cs`**
   - 統計計算: ~~`o.ShopifyCreatedAt ?? o.CreatedAt`~~ → `o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt`

4. **`DormantSegmentationService.cs`**
   - セグメント分析: ~~`o.ShopifyCreatedAt ?? o.CreatedAt`~~ → `o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt`

### 期間集計関連（すべて対応完了 ✅）

1. **`DashboardController.cs`**
   - 日次売上集計: ~~`(o.ShopifyCreatedAt ?? o.CreatedAt).Date`~~ → `(o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt).Date`

2. **`YearOverYearService.cs`**
   - 前年同月比分析: ~~`(o.ShopifyCreatedAt ?? o.CreatedAt).Year/Month`~~ → `(o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt).Year/Month`

3. **`MonthlySalesService.cs`**
   - 月次売上集計: ~~`(o.ShopifyCreatedAt ?? o.CreatedAt)`~~ → `(o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt)`

4. **`PurchaseCountDataService.cs`**
   - 購入回数分析: ~~`(o.ShopifyCreatedAt ?? o.CreatedAt)`~~ → `(o.ShopifyProcessedAt ?? o.ShopifyCreatedAt ?? o.CreatedAt)`

---

## ⚠️ 問題点（2026-01-22 解決済み）

### ~~現在の実装の問題~~

~~1. **`processedAt`を取得していない**~~
   ~~- Shopify REST APIの`created_at`のみを取得~~
   ~~- `processedAt`はShopify Admin GraphQL APIのフィールドのため、REST APIでは取得不可~~

**【2026-01-22 訂正】** 上記は誤りでした。REST APIにも`processed_at`は存在し、取得可能です。

2. **`createdAt`と`processedAt`の違い**
   - `createdAt`: システム的に注文が作成された日時（基本的に不変）
   - `processedAt`: 注文が処理された日時（注文の処理日時で可変、分析レポートなどで利用される）

~~3. **過去データ投入時の問題**~~
   ~~- Postmanで`processedAt`を任意の過去日時に設定しているが、EC Rangerアプリは`createdAt`を参照している~~
   ~~- そのため、期待した形でデータが表示されない可能性がある~~

**【2026-01-22 解決】** `processedAt`を取得・保存するよう対応完了。データ再同期後は期待通りに表示されます。

---

## 🔧 対応方針（2026-01-22 対応完了）

### ~~オプション1: `processedAt`を取得・保存する（推奨）~~ ✅ 採用・実装完了

**メリット**:
- 分析レポートなどで使用される`processedAt`を正確に反映
- 過去データ投入時の期待通りの表示が可能

**実装内容**:
1. ~~Shopify Admin GraphQL APIを使用して`processedAt`を取得~~ → Shopify REST APIから`processed_at`を取得（GraphQL不要）
2. `Order`エンティティに`ShopifyProcessedAt`フィールドを追加 ✅
3. 分析処理で`ShopifyProcessedAt ?? ShopifyCreatedAt ?? CreatedAt`を使用 ✅

~~**注意点**:~~
~~- Shopify REST APIからGraphQL APIへの移行が必要~~
~~- または、REST APIとGraphQL APIの両方を使用~~

**【2026-01-22 訂正】** GraphQL対応は不要でした。REST APIで`processed_at`が取得可能です。

### ~~オプション2: 現在の実装を維持（`createdAt`を使用）~~ ❌ 不採用

---

## 📝 ~~推奨対応~~ 対応完了（2026-01-22）

### ~~推奨: オプション1（`processedAt`を取得・保存）~~ ✅ 実装完了

**理由**:
1. ユーザーが`processedAt`を任意の過去日時に設定している
2. 分析レポートなどで`processedAt`が使用される
3. 期待した形でデータが表示されるようになる

**実装ステップ**:
1. ✅ `Order`エンティティに`ShopifyProcessedAt`フィールドを追加
2. ✅ マイグレーションを作成（`2026-01-22-AddShopifyProcessedAtToOrders.sql`）
3. ~~Shopify Admin GraphQL APIから`processedAt`を取得~~ ✅ Shopify REST APIから`processed_at`を取得
4. ✅ 分析処理で`ShopifyProcessedAt`を優先的に使用

---

## 🔗 参考情報（2026-01-22 訂正）

### Shopify API ドキュメント

~~- **REST API**: `created_at`フィールドのみ~~
~~- **Admin GraphQL API**: `createdAt`と`processedAt`の両方が存在~~

**【2026-01-22 訂正】**:
- **REST API**: `created_at`、`updated_at`、**`processed_at`** のすべてが存在
- **Admin GraphQL API**: `createdAt`、`updatedAt`、`processedAt`が存在

### 現在の実装（2026-01-22 更新後）

- **使用API**: Shopify REST API
- **取得フィールド**: `created_at`、`updated_at`、`processed_at`
- **保存フィールド**: `ShopifyCreatedAt`、`ShopifyUpdatedAt`、`ShopifyProcessedAt`

---

## 📚 関連ファイル

- `backend/ShopifyAnalyticsApi/Services/ShopifyApiService.cs` - Shopify APIからの取得
- `backend/ShopifyAnalyticsApi/Jobs/ShopifyOrderSyncJob.cs` - データベースへの保存
- `backend/ShopifyAnalyticsApi/Models/DatabaseModels.cs` - データベーススキーマ
- `backend/ShopifyAnalyticsApi/Services/Dormant/DormantCustomerQueryService.cs` - 休眠顧客判定
- `backend/ShopifyAnalyticsApi/Controllers/DashboardController.cs` - 期間集計

---

## 📁 マイグレーション

- **ファイル**: `docs/05-development/03-データベース/マイグレーション/2026-01-22-AddShopifyProcessedAtToOrders.sql`
- **内容**: `Orders`テーブルに`ShopifyProcessedAt`カラムを追加
